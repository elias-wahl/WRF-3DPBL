# Pass-over 2026-09-16 — the soil spin-up (A28) is running; next session picks up here

Read this first; details in `DECISIONS.md` (2026-09-15 ~14:30 → 2026-09-16 ~10:15) and `branko/OPEN_ISSUES.md` A28.

## Where the science stands
- **A28: the ICON forcing's soil state is wrong** — 3–7 K too cold below the top centimetres (probes: Kolsass 19.5 °C at
  5 cm vs model 13.0; Eggen 17.8/12.0; StanserJoch 11.0/7.5; ICON's own GRIB column 18.8/17.2/15.6 °C at 6/18/54 cm) and
  9 vol-% too dry at the floor (22 vs 31). Every WRF run inherits it: nocturnal ground flux 20–25 W m⁻² short (the air
  pays, HFX 2–5× too negative, drainage 1.5–2.5× too fast), daytime sensible heat flux at the floor 4× observed.
- Sidewall longwave is NOT the cause (Weerberg net −63 obs / −60 model). The day-spun chain runs a 3 K warm floor skin
  with a soil still 2 K cold → a temperature-only soil patch is unsafe; the fix is a spun-up land state (Elias).
- Instrument traps: sonic `mean_t` is not a thermometer (E50); HATPRO is warm above ~800 m AGL (E51).

## What is running (queue, 2026-09-16 10:30)
- **Job 8632206 `hrldas_spinup`** (zen3_0512, 1 node × 64 ranks, 5 h wall): HRLDAS v5.1.1 + branko Noah-MP 5.1.1,
  2025-04-01 00 → 07-18 00 UT (2592 h), two passes (`SPINUP_LOOPS = 1`), daily output + daily restarts, forcing =
  ICON 500 m hourly series on the WRF grid. Dir `$DATA/hrldas_runs/spinup2025`; restarts land in **`LDASOUT/`**
  (`RESTART.YYYYMMDDHH_DOMAIN1`), the second pass overwrites the first. Expected ~3 h of run time.
  HRLDAS ends without MPI_Finalize → mpirun prints "exited improperly" after a complete run (cosmetic); the batch
  script still exits 0. **128 ranks segfault at init with the April (snowy) setup; 64 ranks work** — keep 64.
- Nothing else queued. Three follow-up jobs for 16–18 July were submitted by different hands and all cancelled
  (two had the restart path wrong: it is `spinup2025/LDASOUT/RESTART...`, not `spinup2025/RESTART...`).

## What to do next, in order
1. **When 8632206 is done** (`sacct -j 8632206`; `ls hrldas_runs/spinup2025/LDASOUT | tail`): check the last daily
   output at Kolsass (y=259, x=285) — SOIL_T dims are (Time, south_north, soil_layers, west_east) — against the probes
   (5 cm 19.5 °C night / 21.5 °C day on 17–18 July; soil moisture ~0.32 at Kolsass, ~0.19 at Eggen; StanserJoch 11 °C).
2. **Hourly 16–18 July segment** for the i-Box flux comparison and the WRF-twin restarts: `hrldas_runs/spinup2025_july`
   has the correct namelist (restart `spinup2025/LDASOUT/RESTART.2025071600_DOMAIN1`, START 07-16 00, KHOUR 48,
   OUTPUT_TIMESTEP 3600, RESTART_FREQUENCY_HOURS 1). Submit it: `cd hrldas_runs/spinup2025_july && sbatch run_hrldas.slurm`
   (64 ranks, ~1 min of compute). `hrldas_runs/july1618` is the same thing with the wrong restart path — delete it.
3. **Validate before any WRF run:** HRLDAS `HFX`/`LH` at Kolsass and Weerberg on 17–18 July vs the i-Box EC
   (`data/stations/ibox_ecpy/*_toc_ecpy_30min_2025.csv`, `sh1`/`le1`; obs Kolsass 10–14 UT: H 47, LE 260, Bowen 0.2;
   Weerberg H 126, LE 204) and `SOIL_T`/`SOIL_M` vs the probes (`slope_soil_check.py` shows the probe columns).
   Gate: Bowen ratio at Kolsass near 0.2 with the spun-up soil → the soil was the problem; still ~0.8 → the
   land-surface parameters are.
4. **WRF twin of X16b** (`X16s`): write `TSLB SMOIS SH2O TSK SNOW SNOWH SNOWC CANWAT` from the HRLDAS restart of
   2025-07-17 21 UT into a copy of `branko_runs/innval_pbl3d_X16b/wrfinput_d01` (a small script; HRLDAS restart names:
   `SOIL_T`, `SOIL_M`, `SH2O`, `TG`/`TV`…, check with ncdump), then run as X16b was (5 × 128, 3:15, `mpirun`,
   full output streams, `output_t_fluxes = 1`). Gates: slope HFX → −13…−15 W m⁻², ground flux → −41…−47, LW↑ at
   Weerberg/Hochhäuser, drainage at Eggen/StanserJoch, HATPRO 21→01 cooling (−3.4/−3.1 K), X16w θ budget terms.
5. The chain start (13 UT) uses the 13 UT restart the same way.

## Tools and files made this week
- `icon2wrf` main: `run_surface_series.sh START END [OUT] [JOBS] [WRFINPUT|grid.txt|none]` → one hourly NetCDF
  (HRLDAS names) on the WRF grid; `src/icon2wrf/surface_series.py`. Product: `$DATA/ICON/surface_series/
  icon_surface_2025040100_2025071800.nc` (71 GB, 2593 h, no gaps; 24 h diurnal-filled 05-11/12; ~60 h interpolated,
  17 July all real). Also on LEO4 (`ssh leo4`, key set up; `/scratch/c7071156/icon2wrf/output/`).
- `$DATA/hrldas`: HRLDAS v5.1.1 built serial + MPI (`hrldas/run/hrldas_serial.exe`, `hrldas.exe`); `noahmp/` is a
  copy of `branko/phys/noahmp` (MVT=33 fix pushed to the fork, branch `corine-33cat`, submodule bumped in branko).
  `tools/make_ldasin.py` (series → hourly LDASIN files), `tools/make_setup.py` (wrfinput + ICON land state at t0).
- `wrf3dpbl-diag`: `sfclay5_night_check.py`, `x16_theta_budget.py`, `x16w_slope_diag.py`, `slope_radiation_check.py`,
  `slope_soil_check.py`, `plot_radiation_stations.py` (figure `plot_output/diagnostics/radiation/ibox_radiation_1718.png`).
- Station data added: `data/stations/ibox_rad/` (CNR4 at Weerberg, Hochhäuser, Arbeser; raw at Eggen, StanserJoch —
  their LW columns are uncalibrated mV). Rule: never send mails / use the Gmail connector without asking.

## Standing rules that changed
- Full output streams and `output_t_fluxes = 1` on every experiment until production.
- `submit_wrf.slurm` now creates `$WRF_OUTPUT_ROOT/temp/branko` (E49); check the initial surface state against every
  station variable before any physics hypothesis (E52, RULE-003).
