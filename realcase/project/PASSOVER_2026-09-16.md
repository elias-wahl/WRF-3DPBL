## 2026-09-25 ~11:45 — two HATPROs (DECISIONS ~11:45)

Radfeld (MeteoSwiss) vs Kolsass (UIBK), E59 boxes, hydrostatic θ: afternoon 13–17 UT bias ≈ 0 at Radfeld but +0.4…+0.5 K at Kolsass (+1.8 K at 17 UT) in both closures → the afternoon anomaly is Kolsass-local; night 22–05 UT +2.9…+3.2 K at Radfeld, +3.4…+3.9 at Kolsass (0–100 m) → the night bias is valley-wide and not seeded at Radfeld by an afternoon anomaly. At Radfeld the model misses the deep (300–600 m) evening cooling. Script `wrf3dpbl-diag/hatpro_radfeld_check.py`.

## 2026-09-25 ~11:00 — HATPRO θ with the measured station pressure (E72)

The loader in `wrf3dpbl-diag/hatpro_cooling.py` now uses the i-Box pressure instead of a fixed 950 hPa: HATPRO θ −0.3…−0.5 K, model − HATPRO biases +0.3…+0.5 K larger (01 UT +0.47); sonde-referenced numbers unchanged. The UIBK HATPRO stood next to the Kolsass tower (Elias). A second one, MeteoSwiss's HATPRO-G5 at Radfeld Klärwerk (47.459 N 11.932 E, 510 m), is now on disk for July: `data/stations/rad/hatpro_meteoswiss/` (Zenodo 10.5281/zenodo.21918290, README there; θ from its MET `Surf_P`).

## 2026-09-25 09:40 — implicit θ/qv solver: both gates passed; HSI2 8668069 queued (crashed config + both implicit, 01→07 UT)

HERO segment b 8667814 / link c 8667815 still held. HSI1 (scalar implicit only) built, deferred (later: can `pbl3d_sq_implicit` be dropped?).

## 2026-09-25 10:05 — implicit θ/qv solver implemented, rebuild running; both implicit switches on from now on

Rebuild PID in `branko/.build_login.pid` (log `.build_scalar_implicit.log`). After it: gate 1 SMOKEDEF (defaults, bit-for-bit vs 8659688), gate 2 SMOKESI (switch on, devel), then HSI2 (crashed config + both implicit, cold 01→07). HSI1 (scalar only) built, deferred — **later: test whether `pbl3d_sq_implicit` can be dropped**. HERO segment b 8667814 / link c 8667815 still held.

## 2026-09-25 08:45 — HERO segment a done; segment b 8667814 and link c 8667815 HELD by Elias's instruction

Release: `scontrol release 8667814 8667815`. Segment b = 07→13 UT, class-19 roughness 0.05, restart from HERO's 07:00 file. Open for Elias: the numerical fix for A31 (DECISIONS 06:05, 08:30).

## 2026-09-25 06:05 — problem found (A31: explicit scalar-flux instability of the closure); HERO segment a running with the scalar back-off; numerical fix awaits Elias

**Queue:** HERO segment a 8667361 running (past 04:17 UT at 05:48 clock; ends ≈ 08:20 clock) → chain link b 8667362 `afterok`. HC2 and HR0B finished. **Decision for Elias:** which numerical fix for A31 (implicit vertical scalar transport / flux-divergence limiter / effective-diffusivity `dn_max`) — DECISIONS 2026-09-25 06:05. If HERO fails again, the in-session wait resumes the diagnosis.

## 2026-09-25 02:55 — loop iteration 2: cold-start single-switch tests; HERO segment a is one of them

**Queue:** HERO segment a 8667361 (01→07 UT, 2 × 128, 6:15, `pbl3d_t2_scalar = 1`) → chain link b 8667362 `afterok`; HC2 8667359 (crashed config, class-19 roughness 0, 01→04:10); HR0B 8667360 (devel, restart-fidelity test). In-session waits armed on all three. Restart twins HR0–HR4 finished clean (the control too → restart not faithful, E70). DECISIONS 2026-09-25 02:55.

## 2026-09-25 02:00 — crash reproduced and explained as a numerical mode; five restart twins queued

**Queue:** HR0 8667299 (control), HR1 8667300 (`pbl3d_t2_scalar = 1`), HR2 8667301 (`pbl3d_dn_max = 0.4`), HR3 8667302 (class-19 `Z0MVT` = 0), HR4 8667303 (`diff_6th_slopeopt = 1`) — 2 × 128 on `zen3_1024`, restart from `branko_runs/innval_pbl3d_HDIAG/wrfrst_d01_2025-07-17_03:30:00`, to 04:10 UT. Mechanism and decision rule: DECISIONS 2026-09-25 02:00, OPEN_ISSUES A31. HERO not queued (namelist ready: `run_days 0`, `t2_scalar 1`).

## 2026-09-25 00:55 — HERO crash diagnostic loop, armed IN-SESSION (Elias: self-restart when a diagnostic run finishes or a run fails)

**Mechanism.** The cron/headless self-restart pipeline was refused by the auto-mode classifier twice (also the settings rule; `/permissions` is not available from the remote view). Instead the live session (conversation `8b06c42c-b11b-4aa8-8f84-d0f16cf153b8`, CLI process on l55) waits on SLURM jobs with background tasks and is re-invoked when they end. **It survives only while that CLI process lives**; if it dies, run `cinit` and continue from this block. A durable version needs Elias to add an allow rule in `~/.claude/settings.json` at a terminal.

**Loop (Elias 2026-09-25 00:40, verbatim intent: "do diagnoses to see what was the cause for the crash. If you find it restart HERO, if not start another diagnostic run to help. Do this until you find the problem ... if HERO fails again ... start again with your diagnostic loop").**
1. When a diagnostic run ends: read its report / scan its 1-min and per-step frames (`exp/HDIAG/temp/branko/diag1min_*`, `diagstep_*`, or `wrf_output/<id>/`): first non-finite field, cell, level, step; the growth before it (closure moisture/heat flux, condition number, realizability flags, microphysics, filter gate, sunrise heating, urban temperatures).
2. **Cause found** = a single switch removes the blow-up in a restart twin from HDIAG's 03:30 restart that runs ≥ 20 min past the crash time, while the identical control twin reproduces it. Twins via `setup_restart_run.sh` (sets `run_days = 0`, `override_restart_timers`); add `TURB_FLUX_WQV`, `TURB_FLUX_WTHETA_V`, `PBL3D_COND_M` to the stream. Candidate order: control; `pbl3d_t2_scalar = 1`; `diff_6th_slopeopt = 1`; class-19 `Z0MVT` 0.30 → 0 (own table copy, E60).
3. Found → HERO (namelist already `run_days 0`, `t2_scalar 1`) with the fix; a fix that removes a hero design element is applied only if that element is shown to be the cause, and flagged for Elias at the top of this file; code changes only through the standing gates (rebuild, bit-for-bit on defaults, devel smoke). Submit segment a from inside its dir, chain link `hero_chain.slurm` `afterok` (RUN=HERO, IDX=b), and a background wait on every segment: on FAILED/TIMEOUT/NODE_FAIL/OOM go back to step 1.
4. Not found → submit the next diagnostic set, background-wait on it, record, continue.
5. Every iteration: DECISIONS + this file + memory, sync, commit, push; rate 1–10. **Stop and hand to Elias** when three iterations in a row add no new measured fact, the same setup error recurs twice, HERO fails three times, or 30 node-hours of diagnostics are spent. Never cancel jobs not created by the loop (E54); nothing from the CLAUDE.md "Do NOT" list.

## 2026-09-25 00:45 — HERO namelist fixed; diagnostic run in flight; no automatic restart armed

**Queue:** 8667065 `wrf_HDIAG` (running since 22:47; crash expected near 01:35 clock, else TIMEOUT near 02:07) → 8667067 `hdiag_rep` (`afterany`). HERO not queued; its namelist now has `run_days = 0` and `pbl3d_t2_scalar = 1`, and `hero_chain.slurm` resets `run_days`. The self-restart loop Elias asked for is NOT armed (blocked by the auto-mode classifier; DECISIONS 00:45). Next session: read `exp/HDIAG/hdiag_report.<id>.log` and the per-step frames `exp/HDIAG/.../diagstep_*`, then decide restart twins from HDIAG's 03:30 restart.

## 2026-09-24 23:55 — diagnostic reproduction of HERO's crash queued

**Queue:** 8667065 `wrf_HDIAG` (HERO segment a reproduction, 01→04 UT, 2 × 128, `zen3_1024`, 1-min stream from 03:30, per-step stream 03:49–03:51, restart at 03:30) → 8667067 `hdiag_rep` (`afterany`, writes `exp/HDIAG/hdiag_report.<id>.log`). HERO itself is not queued. DECISIONS 23:30 (offline analysis) and 23:55 (this run).

## 2026-09-24 22:30 — HERO crashed again, in longwave radiation at 03:50 UT; nothing queued

Segment a 8666860 ran 01:00→03:50 and segfaulted in RRTMG-LW `taumol` on five ranks (DECISIONS 22:30); the 03:30 frame is sane. Chain link cancelled. No restart file. Next (proposed, not approved): rerun segment a on the same 2 × 128 layout with `restart_interval = 30` and a 1-min stream from 03:30 to catch the column.

## 2026-09-24 19:40 — HERO requeued without the equilibrium start

**Queue:** 8666860 `wrf_HERO` (segment a, 01→07 UT 17 July, 2 × 128, 5:15, `zen3_1024`, projected 25 Sep 00:20) and chain link 8666861 (`afterok`, builds b and so on; judges after d and h). Closure keys now `init_opt 0 / l0_min 0 / moist_cond_max 1e4` in HERO and in the template (DECISIONS 19:40).

## 2026-09-24 19:00 — HERO crashed; cause found; nothing queued

**Queue: empty.** HERO segment a (8665306) died at 01:01:12 sim time with `SFCLAYREV produced NaN`; its chain link 8665307 is cancelled. Seven devel twins (`branko_runs/innval_pbl3d_HD0…HD6`, ids in `branko_runs/HD_devel_jobs.txt`) pin it on **`pbl3d_init_opt = 1`**: the level-2 start seeds q² of 500–1600 m² s⁻² at 4–10 km and the closure runs away aloft within a minute (DECISIONS 2026-09-24 18:55, KNOWN_ISSUES E68). Filter option 5, the urban canopy, the l0 floor and the moist cap are each excluded by a twin. **Waiting on Elias:** requeue HERO with `pbl3d_init_opt = 0` (clean in the devel test) or repair the equilibrium start first. `clone_twin_rundir.sh` now copies a parent's own tables (E67).

## SESSION END 2026-09-21 — mechanism found, two options implemented, pair in the queue

**Queue right now (updated 2026-09-21 16:00):** the twins were moved twice. 8653060/61 (`zen3_2048`) were cancelled; 8653959/60 (`zen3_1024`, 5 x 4:00) started ~1 h after submission and died after 13 s (cause not on disk — the run dirs were rebuilt); 8655625 `wrf_X26` / 8655626 `wrf_X27` (5 x 4:00, `zen3_1024`) were pending with a scheduler-projected start of **2026-09-24**. `sbatch --test-only` showed the lever is node count, not wall time (all lanes have no holes; 283 of 638 `zen3_0512` nodes drained, 80 reserved to 30 Sep): a 3-node job on `zen3_1024` is projected for **2026-09-22 02:22**, 5 nodes for the 24th, wall 6:00/6:30/7:00 identical. Both `submit_wrf.slurm` headers are now **3 nodes x 6:30** (9 h sim at ~1.1 s/step ≈ 5.4 h; E14: X26 is no longer bit-comparable to X22's 5-node decomposition, the pair is compared statistically anyway). `branko_runs/requeue_X26_X27_3nodes.sh` cancels 8655625/26/27, resubmits both and re-arms the judge (`exp/x16_judge/judge_X26_X27.slurm`, `afterany`); it logs the new ids to `requeue_X26_X27_3nodes.log`. Run 2026-09-21 16:20: **X26 8656316, X27 8656317, judge 8656318**.

**The mechanism, end to end (all measured this session).** At 13:00 UT WRF and ICON are identical over the
range in wind, cross-range gradient and every spectral band. Within fifteen minutes WRF more than doubles its
v variance at 2-6 dx (0.194 -> 0.408 m2/s2) and adds 71 % at 6-12 dx, below and around its own effective
resolution, over the steep slopes where the sixth-order filter is tapered off; ICON's 2-6 dx variance falls
over the whole hour. |grad v| sharpens 57 %, -v dv/dy doubles, the northerly accelerates 2.6x faster than
ICON's, over-runs the range, descends the sunlit lee slope, destroys the buoyancy barrier ICON keeps
(+1.79 K), plunges to the valley floor, and after sunset warms it adiabatically at +7 to +22 K/h -- the
evening and nocturnal warm bias. Terrain and low-level blocking are excluded by X12mta, which carries ICON's
terrain, reproduces its Nh/U = 2.04 and dividing-streamline height 512 m exactly, and still breaks in.

**Two retractions this session, both from comparing resolved quantities without their subgrid partner
(KNOWN_ISSUES E63):** the "WRF's sigma_w and crest momentum flux are 1.8x/1.5x ICON's" line, and with it the
"momentum sink through depth over the range" hypothesis that had stood since 09-18. Totals agree to 16 % and
~5 %. Across nine runs the crest flux is anti-correlated with the plunge.

**What was built.** Branch `3dpbl_lh_and_filtergate` (commits 9e8780f0d, 7b4061900), rebuilt on the login
node, **bit-for-bit identical to X22 on defaults across 332 variables at the same 5-node layout**:
- `diff_6th_slopeopt = 3` -- gate the filter's slope taper on static stability, evaluated on the most stable
  cell of the 5th-difference stencil. Offline: 95 % of the untapered action at 13:30, the production taper's
  theta cost to three decimals at 00:00. New real `diff_6th_stabthresh`, default 2 K/km.
- `pbl3d_lh_opt = 1` -- restore in the horizontal only the length the grey-zone taper removed,
  `l_h = l_master (1-Psig_bl)/Psig_bl`, `K_h = 0.39 q l_h`, applied to the horizontal components and not to
  the q^2 budget. No new constant. NOT the literal 3DTKE formulation -- see DECISIONS 2026-09-21 10:30 and
  12:10 for why, and for the three defects found in the goger19 km_opt=5 block.

**Next, in order.** Read the judge log. Judge on: 2-6 dx variance (ICON 0.208, production 0.271), 6-12 dx
(0.194 / 0.382), |grad v|, -v dv/dy, the 17 UT floor break-in (ICON -1.5, production -7.5 m/s), the lee
barrier (ICON +1.79 K), the 01 UT 0-100 m bias, and the two damage controls -- Kolsass up-valley wind
(lidar +5.4 at 17 UT) and the nocturnal theta structure over the slopes. **ICON is the yardstick, not the
target:** it is itself ~2x the observed Stanser Joch wind and too weak at Arbeser.

**Open and offered, not built:** `pbl3d_lh_opt = 2` carrying the actual 3DTKE form as a top-up,
`max(0, K_3DTKE - S_m q l_master)`, so the two constructions can be run as twins.

---

# Pass-over 2026-09-16 — the soil spin-up (A28) is running; next session picks up here

Read this first; details in `DECISIONS.md` (2026-09-15 ~14:30 → 2026-09-16 ~10:15) and `branko/OPEN_ISSUES.md` A28.

## Where the science stands
- **A28: the ICON forcing's soil state is wrong** — 3–7 K too cold below the top centimetres (probes: Kolsass 19.5 °C at
  5 cm vs model 13.0; Eggen 17.8/12.0; StanserJoch 11.0/7.5; ICON's own GRIB column 18.8/17.2/15.6 °C at 6/18/54 cm) and
  9 vol-% too dry at the floor (22 vs 31). Every WRF run inherits it: nocturnal ground flux 20–25 W m⁻² short (the air
  pays, HFX 2–5× too negative, drainage 1.5–2.5× too fast), daytime sensible heat flux at the floor 4× observed.
- Sidewall longwave is NOT the cause (Weerberg net −63 obs / −60 model). The day-spun chain runs a 3 K warm floor skin
  with a soil still 2 K cold → a temperature-only soil patch is unsafe; the fix is a spun-up land state (Elias).
- Instrument traps: sonic `mean_t` is not a thermometer (E50); HATPRO is warm above ~800 m AGL (E51).

## SESSION END 2026-09-20 — read this first

**Nothing is queued. Nothing is uncommitted.** The binary in `branko/main/wrf.exe` is the 2026-09-18 build
(opt-in `pbl3d_l_opt = 5` and `pbl3d_dn_max`; defaults bit for bit, verified against X22's 13:05 frame,
333 variables identical). Previous binary kept as `main/wrf.exe.pre_lopt5`.

**Where the science stands.** The lead problem of the last two weeks — a shallow afternoon northerly that
over-runs the northern-range crests and descends the sunlit lee slope to the valley floor, killing the
up-valley wind and seeding the +3.3 K night bias — now has a measured mechanism: **over the crests 90 % of
the vertical momentum flux is RESOLVED and terrain-locked (stationary over 30 min), and it pumps northerly
momentum from 150–600 m down into the lowest 150 m.** The subgrid flux carries under 10 %. That retires
every parameterisation fix at once and matches every null result on record.

**What was excluded by a run or a like-for-like measurement:** the 3D closure (MYNN twin identical),
surface roughness from 2 mm to 0.30 m (four runs, ICON's own drag level reached), the land-cover heat
partition, the master length scale (doubled, floor got worse), the soil state, the start time, the terrain
data set, the along-valley and cross-range pressure force, the foreland inflow, the mixed-layer depth
(identical, 814 vs 828 m), and any initialisation imbalance (flat-ground excess 0.6 m/s at 14 UT, 0.2 by 16 UT).

**Next tests act on resolved scales, not on physics:** the sixth-order filter over steep slopes
(`diff_6th_slopeopt = 0` took 25–30 % off the lee current in a 2 h twin — needs a NIGHT check before it can
be production, see A20), terrain steepness/smoothing, and the vertical coordinate. Open question: why ICON
does not do this at the same 500 m, its terrain being steeper rather than smoother.

**Tools built this session:** `realcase/scripts/clone_twin_rundir.sh` (namelist/table twins with the E49 and
E60 guards), `wrf3dpbl-diag/crest_momentum_partition.py`, `northerly_snapshot_budget.py`,
`northerly_origin_check.py`, `northerly_onset_timing.py`, `cross_range_forcing_icon_vs_wrf.py`,
`valley_pgf_wind_icon_vs_wrf.py`, `valley_heat_input_icon_vs_wrf.py`, `breakin_check.py`. All logs in
`exp/x16_judge/`; every log's first lines state how model and observation were compared.

**Standing instruction added 2026-09-19:** the Anki card step is suspended until Elias re-enables it.

## What is running (queue, 2026-09-20 09:15): nothing
- **Mechanism found (DECISIONS 2026-09-20 09:15):** the vertical momentum flux that builds the crest jet is 90 % RESOLVED and terrain-locked (stationary over 30 min); it pumps northerly momentum from 150–600 m into the lowest 150 m, giving a surface wind maximum where ICON has a friction profile. Mixed-layer depths are identical (ICON 814 m, WRF 828 m) and the models agree above 400 m. No closure/roughness/land-cover knob can touch it. Next tests act on resolved scales: filter over steep slopes, terrain steepness, coordinate.

## Before that: What is running (queue, 2026-09-18 14:58): nothing
- Filter twins judged (DECISIONS 2026-09-18 14:58): filter off = production; filter on over steep slopes takes 25–30 % off the lee-slope current, no regime change. Remaining suspects for the excess northerly made over the range in the first hour: missing drag/momentum mixing among the peaks, first-hour adjustment of ICON's state, coordinate pressure-gradient error.

## Before that: What is running (queue, 2026-09-18 12:50): nothing
- **X20 judged** (DECISIONS 2026-09-18 12:50): forest partition on target, reach heat −15 of +62 W m⁻², atmosphere unchanged → the land surface is a minor contributor; next suspect = WRF's first-hour adjustment of the crest/foreland northerly (crest air −0.5…−0.9 K vs ICON from 14 UT).

## Earlier (2026-09-18 08:55)
- All three overnight runs completed and judged (DECISIONS 2026-09-18 ~08:55): **X17m** (MYNN twin) has the same lee-slope
  break-in → not the 3D closure; **X19r** (class-19 z0 0.20 m) no effect → roughness closed; **X17n** (clear sky from 19 UT)
  buys 0.3–0.6 K of the 2–3 K evening floor bias. New lead: the WRF valley floor over-warms +1.3 K vs ICON by 16 UT in both
  closures, before the floor break-in — the common root for break-in and evening bias; suppliers separated (DECISIONS 2026-09-18 09:03): momentum exonerated to 16 UT, the
  surface sensible heat is the supplier — needleleaf-forest cells, Bowen 1.9 vs ICON 0.5, slope sonics 2–3× → **A30**. Logs in `exp/x16_judge/` (`X17m_X19r_breakin_check.log`,
  `lee_floor_theta_X17_X17m_vs_ICON.log`, `X17n_evening_arrival.log`), script `wrf3dpbl-diag/breakin_check.py`.

## What to do next, in order
0. **Lead finding 2026-09-17 evening (DECISIONS, entry 'THE THREE VALLEY-WIND DIAGNOSTICS'):** [pressure-gradient claim RETRACTED 20:50 — at fixed heights WRF holds ICON's gradient; what stands:] on 17 July a northerly floods the upper valley over the northern ranges
   from 16 UT (Innsbruck 8–9 m/s from N vs observed easterly); ICON does not do it; ICON terrain in WRF recovers the daytime
   gradient but not the break-in. Next: cross-sections of the northerly across the Karwendel/Seefeld/Achensee at 15–17 UT, WRF vs
   ICON; treat the 17th's evening as unfit for closure tests, prefer the 18th.
1. DONE 2026-09-17 (DECISIONS 15:30, 16:15): initial state clean aloft; along-valley θ flat; floor warm advection = subsidence (slope-circulation return branch); filter ~0.06 K/h at Kolsass; ICON's own floor cools −0.45 K/h vs WRF −0.28 vs obs −0.86. Hypotheses 2/3 (slope drainage + laminar core) carry the deficit. Previously: initial state checked (DECISIONS 2026-09-17 15:30): ICON at 21 UT is on HATPRO aloft (+0.2 K at 100–300 m, +0.9 K at 0–100 m) and
   on the lidar; the deficit opens 21:00–22:30 while X16s turns to a westerly down-valley flow (1.2 m/s from 272° at 50–150 m at 22 UT)
   where the lidar keeps a weak easterly (from ~95°) up to 300 m. NEXT: θ along the valley axis at 21 UT and in the first frames
   (is the western reach warmer?), the along-valley pressure force and momentum budget on X16s (`evening_momentum_budget.py X16s`,
   `valley_transect.py X16s …`), the lidar wind hour by hour (`icon_init_check.py` covers 20:30–22:30).
2. Drainage onset checked (DECISIONS 17:15): sonics show drainage established from 19–20:30 UT; the 21 UT cold start has none and X16s
   overshoots it 1.5–2× within 30 min → the floor's early warm subsidence is a cold-start transient; X17 is the test. Then item 4: slope drainage path budget and q² vs the slope sonics on X16s (crest→floor at ~9 m; where the drainage detaches, how it is diluted; the 150–300 m layer that the observations cool at −0.77 K/h under a 1.5 m/s easterly the model kills). Then item 5 (closure sensitivity twin at the drainage top). The X16w programme on X16s (`x16w_slope_diag.py X16s` section B/C is in `X16s.log`; the
   drainage detachment, dilution on the slope, along-valley import) — now without the surface confounder.
2. Land-surface parameters (first order for LW↑/T2 at the floor, second order for the pool): meadow land-use class for the
   i-Box meadows (LU 2 'dryland cropland' at Kolsass/Weerberg; forest cells at Hochhäuser/StanserJoch), ground–canopy
   insulation (top-layer swing 40 % too large, skin +2 K at night), loam hydraulics (28 vs 32 vol-%).
3. Chain start (13 UT) from `RESTART.2025071713_DOMAIN1` (winter run) via `make_twin_rundir.sh <13 UT parent> <name> …` when a
   daytime twin is wanted; grep the submit script for the E49 `mkdir` before `sbatch`.

## What the gate showed (DECISIONS 2026-09-16 11:15)
- Offline Noah-MP with the spun-up soil, ICON atmosphere, Kolsass: night skin +1 K, HFX −3 vs sonic −3, net LW 51 vs 56,
  ground flux −60 vs observed residual −48 (chain: 20–25 short); day Bowen 0.38 vs 0.43 observed (chain 0.8 with ICON's 22 %).
  → A28's soil state is the mechanism behind the chain's floor errors. Remaining: the 0–10 cm layer swings 40 % too much and
  ends the night 2–3 K colder than the 5 cm probes at all sites (ground–canopy insulation, a parameter, second order).
- Spun-up minus ICON (land mean): +2.2…+3.3 K per layer, +1.3…+3.3 vol-%. Pass 2 − pass 1: +0.1 (top) … +0.9 K (1–2 m):
  deep layers not converged — a production spin-up wants real forcing from the previous autumn (soil-moisture memory).
- Slope cells (Eggen, Weerberg, StanserJoch, Hochhäuser) keep H 1.3–4× the meadow towers: their land use is mosaic/forest — a
  land-cover question, not soil.

## What to do next, in order
1. **Judge X16s when 8637042 is done** (`exp/x16_judge/X16s.log`, or run the scripts by hand). Gates: slope HFX → −13…−15 W m⁻², GRDFLX → −41…−47, LW↑ at
   Weerberg/Hochhäuser, drainage at Eggen/StanserJoch, HATPRO 21→01 cooling (−3.4/−3.1 K), X16w θ budget terms; compare
   with X16b statistically (E14). The stations to read are in `slope_soil_check.py`, `slope_radiation_check.py`,
   `sfclay5_night_check.py`, `x16_theta_budget.py` (needs the WRFlux stream, on).
2. The chain start (13 UT) uses `spinup_winter_july/LDASOUT/RESTART.2025071713_DOMAIN1` (the winter state) the same way (`make_twin_rundir.sh <13 UT parent> <name> …`,
   its own env file `vsc5_<name>.sh`).
3. **Done 2026-09-17:** the December-start production spin-up (W_SNOW and soil ice in the setup). Still open: meadow land-use
   class, ground–canopy insulation, loam hydraulics (the parameter side of A28).

## Tools and files made this week
- `icon2wrf` main: `run_surface_series.sh START END [OUT] [JOBS] [WRFINPUT|grid.txt|none]` → one hourly NetCDF
  (HRLDAS names) on the WRF grid; `src/icon2wrf/surface_series.py`. Product: `$DATA/ICON/surface_series/
  icon_surface_2025040100_2025071800.nc` (71 GB, 2593 h, no gaps; 24 h diurnal-filled 05-11/12; ~60 h interpolated,
  17 July all real). Also on LEO4 (`ssh leo4`, key set up; `/scratch/c7071156/icon2wrf/output/`).
- `$DATA/hrldas`: HRLDAS v5.1.1 built serial + MPI (`hrldas/run/hrldas_serial.exe`, `hrldas.exe`); `noahmp/` is a
  copy of `branko/phys/noahmp` (MVT=33 fix pushed to the fork, branch `corine-33cat`, submodule bumped in branko).
  `tools/make_ldasin.py` (series → hourly LDASIN files), `tools/make_setup.py` (wrfinput + ICON land state at t0),
  `tools/hrldas_to_wrfinput.py` (restart → wrfinput land state), `tools/make_twin_rundir.sh` (X16b → X16s twin dir); all four
  mirrored in `branko/realcase/scripts/hrldas_tools/`.
- `wrf3dpbl-diag`: `hrldas_check.py` (the A28 gate: probes + EC vs HRLDAS output, daily or hourly mode; `--loop N` for a spin-up pass), `hrldas_pass_compare.py` (ICON soil / pass 1 / pass 2 / probes + ICON's own fluxes), `sfclay5_night_check.py`, `x16_theta_budget.py`, `x16w_slope_diag.py`, `slope_radiation_check.py`,
  `slope_soil_check.py`, `plot_radiation_stations.py` (figure `plot_output/diagnostics/radiation/ibox_radiation_1718.png`).
- Station data added: `data/stations/ibox_rad/` (CNR4 at Weerberg, Hochhäuser, Arbeser; raw at Eggen, StanserJoch —
  their LW columns are uncalibrated mV). Rule: never send mails / use the Gmail connector without asking.

## Standing rules that changed
- **Every future WRF run takes its land state from the full offline spin-up** (winter-start HRLDAS run; restarts in
  `hrldas_runs/spinup_winter_july/LDASOUT/RESTART.<YYYYMMDDHH>_DOMAIN1`, daily 21 UT ones in `spinup_winter/LDASOUT`) through
  `make_twin_rundir.sh` / `hrldas_to_wrfinput.py`; a raw real.exe land state is a named exception. Elias 2026-09-17; DECISIONS 14:55.
- Full output streams and `output_t_fluxes = 1` on every experiment until production.
- `submit_wrf.slurm` now creates `$WRF_OUTPUT_ROOT/temp/branko` (E49); check the initial surface state against every
  station variable before any physics hypothesis (E52, RULE-003).
- HRLDAS traps: the first LDASOUT file of a segment has −9999 fluxes (E55); station-CSV diagnostics need the conda env `proc`,
  not the WRF env's python (E56).
