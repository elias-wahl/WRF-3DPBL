# Pass-over 2026-09-16 — the soil spin-up (A28) is running; next session picks up here

Read this first; details in `DECISIONS.md` (2026-09-15 ~14:30 → 2026-09-16 ~10:15) and `branko/OPEN_ISSUES.md` A28.

## Where the science stands
- **A28: the ICON forcing's soil state is wrong** — 3–7 K too cold below the top centimetres (probes: Kolsass 19.5 °C at
  5 cm vs model 13.0; Eggen 17.8/12.0; StanserJoch 11.0/7.5; ICON's own GRIB column 18.8/17.2/15.6 °C at 6/18/54 cm) and
  9 vol-% too dry at the floor (22 vs 31). Every WRF run inherits it: nocturnal ground flux 20–25 W m⁻² short (the air
  pays, HFX 2–5× too negative, drainage 1.5–2.5× too fast), daytime sensible heat flux at the floor 4× observed.
- Sidewall longwave is NOT the cause (Weerberg net −63 obs / −60 model). The day-spun chain runs a 3 K warm floor skin
  with a soil still 2 K cold → a temperature-only soil patch is unsafe; the fix is a spun-up land state (Elias).
- Instrument traps: sonic `mean_t` is not a thermometer (E50); HATPRO is warm above ~800 m AGL (E51).

## What is running (queue, 2026-09-16 10:45)
- **Job 8632206 `hrldas_spinup`** RUNNING since 10:35 on zen3_0512 (1 × 64 ranks; wall cut 5 h → 3 h, which is what made backfill
  take it — the priority estimate was 23:37). Throughput ~9 s per simulated day → both passes done ≈ 11:10. Restarts land in
  `hrldas_runs/spinup2025/LDASOUT/` (`RESTART.YYYYMMDDHH_DOMAIN1`, pass 2 overwrites pass 1).
- **8632275 `hrldas_july`** (afterok 8632206, 30 min wall): the hourly 16–18 July segment, dir `hrldas_runs/spinup2025_july`
  (restart `spinup2025/LDASOUT/RESTART.2025071600_DOMAIN1`, KHOUR 48, hourly output + hourly restarts). Its log is
  `slurm-8632275.out` (the `--output` line was dropped at submission; fixed in the script for the next time).
- **8632312 `hrldas_check`** (afterok 8632275, devel QOS, report-only): writes `exp/x16_judge/hrldas_check.log` — spin-up daily
  states at the probe sites, July soil + fluxes vs probes/EC, the two twin restarts (13 and 21 UT) listed.
- No WRF job is queued. `hrldas_runs/_DELETE_july1618_wrong_restart_path` is the mis-pathed duplicate — `rm -rf` it (the
  assistant cannot). HRLDAS ends without MPI_Finalize → mpirun's "exited improperly" after a complete run is cosmetic.
  128 ranks segfault with the snowy April setup; keep 64 (E53).

## What to do next, in order
1. **Read `exp/x16_judge/hrldas_check.log`** (or `source ~/miniconda3/bin/activate proc && python wrf3dpbl-diag/hrldas_check.py
   hrldas_runs/spinup2025_july/LDASOUT`; daily states: `… hrldas_runs/spinup2025/LDASOUT --days 4`). The segment ends 18 July
   00 UT (the forcing ends there), so the daytime gate is on the **17th**: obs 10–14 UT Kolsass H 137 / LE 321 W m⁻² (Bowen
   0.43), Eggen 52 / 251 (0.21), Weerberg 123 / 348 (0.35), StanserJoch 126 / 174 (0.72); night 21–00 UT Kolsass 5 cm probe
   19.5 °C, moisture ~32 vol-% (Eggen ~19; StanserJoch 11 °C). Model Bowen near the observed → the soil state was the problem;
   still ~1 (the chain's 0.8 on the 18th) → the land-surface parameters are. Read layer 4 (1–2 m) against the BODEN2 20–21 °C:
   several K low → `TMN` (8.7 °C at Kolsass, the 8 m bottom boundary of Noah-MP, `tbot_opt 2`) is the first suspect — a
   wrfinput field, not a closure constant.
2. **Build the twin (submits nothing):** `hrldas/tools/make_twin_rundir.sh X16b X16s
   hrldas_runs/spinup2025_july/LDASOUT/RESTART.2025071721_DOMAIN1` → `branko_runs/innval_pbl3d_X16s`, output root `exp/X16s`
   (env `realcase/env/vsc5_X16s.sh` exists). Read its namelist diff (output root, `iofields_full.txt`, `output_t_fluxes = 1`
   only), `check_wrfinput.py`, and the empty dangling-symlink list.
3. **Submit** `cd branko_runs/innval_pbl3d_X16s && sbatch submit_wrf.slurm` (5 × 128, 3:15 h as X16b; check the three lanes
   first — vsc5-queue skill). Gates: slope HFX → −13…−15 W m⁻², ground flux → −41…−47, LW↑ at Weerberg/Hochhäuser, drainage
   at Eggen/StanserJoch, HATPRO 21→01 cooling (−3.4/−3.1 K), X16w θ budget terms.
4. The chain start (13 UT) uses `RESTART.2025071713_DOMAIN1` the same way (parent = the 13 UT run dir, its own env file).

## Tools and files made this week
- `icon2wrf` main: `run_surface_series.sh START END [OUT] [JOBS] [WRFINPUT|grid.txt|none]` → one hourly NetCDF
  (HRLDAS names) on the WRF grid; `src/icon2wrf/surface_series.py`. Product: `$DATA/ICON/surface_series/
  icon_surface_2025040100_2025071800.nc` (71 GB, 2593 h, no gaps; 24 h diurnal-filled 05-11/12; ~60 h interpolated,
  17 July all real). Also on LEO4 (`ssh leo4`, key set up; `/scratch/c7071156/icon2wrf/output/`).
- `$DATA/hrldas`: HRLDAS v5.1.1 built serial + MPI (`hrldas/run/hrldas_serial.exe`, `hrldas.exe`); `noahmp/` is a
  copy of `branko/phys/noahmp` (MVT=33 fix pushed to the fork, branch `corine-33cat`, submodule bumped in branko).
  `tools/make_ldasin.py` (series → hourly LDASIN files), `tools/make_setup.py` (wrfinput + ICON land state at t0),
  `tools/hrldas_to_wrfinput.py` (restart → wrfinput land state), `tools/make_twin_rundir.sh` (X16b → X16s twin dir); all four
  mirrored in `branko/realcase/scripts/hrldas_tools/`.
- `wrf3dpbl-diag`: `hrldas_check.py` (the A28 gate: probes + EC vs HRLDAS output, daily or hourly mode), `sfclay5_night_check.py`, `x16_theta_budget.py`, `x16w_slope_diag.py`, `slope_radiation_check.py`,
  `slope_soil_check.py`, `plot_radiation_stations.py` (figure `plot_output/diagnostics/radiation/ibox_radiation_1718.png`).
- Station data added: `data/stations/ibox_rad/` (CNR4 at Weerberg, Hochhäuser, Arbeser; raw at Eggen, StanserJoch —
  their LW columns are uncalibrated mV). Rule: never send mails / use the Gmail connector without asking.

## Standing rules that changed
- Full output streams and `output_t_fluxes = 1` on every experiment until production.
- `submit_wrf.slurm` now creates `$WRF_OUTPUT_ROOT/temp/branko` (E49); check the initial surface state against every
  station variable before any physics hypothesis (E52, RULE-003).
- HRLDAS traps: the first LDASOUT file of a segment has −9999 fluxes (E55); station-CSV diagnostics need the conda env `proc`,
  not the WRF env's python (E56).
