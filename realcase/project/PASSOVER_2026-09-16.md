# Pass-over 2026-09-16 — the soil spin-up (A28) is running; next session picks up here

Read this first; details in `DECISIONS.md` (2026-09-15 ~14:30 → 2026-09-16 ~10:15) and `branko/OPEN_ISSUES.md` A28.

## Where the science stands
- **A28: the ICON forcing's soil state is wrong** — 3–7 K too cold below the top centimetres (probes: Kolsass 19.5 °C at
  5 cm vs model 13.0; Eggen 17.8/12.0; StanserJoch 11.0/7.5; ICON's own GRIB column 18.8/17.2/15.6 °C at 6/18/54 cm) and
  9 vol-% too dry at the floor (22 vs 31). Every WRF run inherits it: nocturnal ground flux 20–25 W m⁻² short (the air
  pays, HFX 2–5× too negative, drainage 1.5–2.5× too fast), daytime sensible heat flux at the floor 4× observed.
- Sidewall longwave is NOT the cause (Weerberg net −63 obs / −60 model). The day-spun chain runs a 3 K warm floor skin
  with a soil still 2 K cold → a temperature-only soil patch is unsafe; the fix is a spun-up land state (Elias).
- Instrument traps: sonic `mean_t` is not a thermometer (E50); HATPRO is warm above ~800 m AGL (E51).

## What is running (queue, 2026-09-18 00:05)
- **X17m 8641792** (MYNN twin of X17a, 13→18 UT: is the lee-slope northerly break-in the 3D closure's or WRF's?), **X19r 8641684**
  (class-19 roughness 0.20 m, restart 16→19 UT), **X17n 8640723** (clear-sky evening twin). Judge with `X19a_breakin_check`-style
  station comparison, `plot_northerly_sections.py <RUN> 11.40 14 16 17`, `evening_arrival_check.py`. DECISIONS 2026-09-17 23:45 onward.
- X17 chain complete (X17a 13–16:30, X17b 16→01, X17c 01→05). X17n (clear-sky twin 19→05) ends ≈ 00:15; X19a (roughness twin) ends ≈ 23:45 —
  its break-in check is already in: break-in persists, crest wind barely responds, towns flag degrades the floor (DECISIONS 23:30).
  NEXT: X17n vs X17b (cloud share of the evening deficit); cross-sections of the northerly over the Karwendel, WRF vs ICON; sound
  version of A29 (heat roughness reduction on the opt-in path, towns roughness-only) only after approval.
- **A29 roughness experiment X19a** (both fixes: class-19 bare-ground roughness 0.05 m via the new opt-in code path; urban classes 31–33
  flagged urban, z0 0.5 m): submitted by gate **8640872** once the bit-for-bit runs 8640869 (old binary) / 8640871 (new) finish on the
  devel queue — read `exp/x16_judge/gate_a29_*.out`; on FAIL nothing is submitted and `main/wrf.exe.pre_A29` is the fallback binary.
  X17c 8640757 and X17n 8640723 running (old binary in memory — unaffected).
- **ICON-terrain lineage reopened:** smoke **X18smoke 8640752** (13 → 14 UT, ICON terrain + production land state; gate for an X18
  chain) and the offline spin-up on the ICON-terrain static fields **8640754 → 8640755** (`hrldas_runs/spinup_winter_icontopo[_july]`).
  Judge the smoke by: completed, no NaN/CFL in `rsl.error.*`, crest winds sane (first-level max < ~20 m/s), 7 frames written.
- **X17n 8640723** (queued): X17b twin from the 19 UT restart with `icloud = 0`, 19 → 05 UT — X17/X12m carry the known spurious
  stratus deck of the native forcing (LW↓ +55 W m⁻² over most of 17–21:30 UT; DECISIONS 2026-09-17 20:10 retracts the 19:30 attribution).
  Compare X17n vs X17b (cloud effect) and vs HATPRO/lidar/sonics (`evening_arrival_check.py X17n=X17a+X17b+X17n …` — note the
  19 UT overlap: list X17n last so its frames win), then the floor θ budget 19–05 UT.
- **X17 daytime-start chain with the production land state:** X17a 8640421 died of a node failure at simulated 16:32 UT (8 frames
  13:00–16:30 archived; daytime gate passed at the floor: Kolsass Bowen 0.32 = obs 0.32; DECISIONS 17:00). **X17b 8640609** (16 → 01 UT from
  `wrfrst_d01_2025-07-17_16:00:00`, 5 nodes, 4:00 wall; built by hand after the generic link failed, E58) → link **8640610**
  (`chain_x17.slurm`) builds and submits X17c (01 → 05 UT). Judge by hand with the X16s scripts on X17b/X17c: first gate = collapse of the up-valley wind vs the lidar
  (20:30–21:00 UT), then θ at 21 UT vs HATPRO, drainage onset vs the slope sonics, the floor budget, HATPRO cooling.
- X16s (8639242) done and judged (DECISIONS 14:45, items 1–2 at 15:30/16:15).
- Winter-start spin-up done (8639481–83; DECISIONS 10:20): equals the April first pass to 0.1 K; winter restarts
  `hrldas_runs/spinup_winter_july/LDASOUT/RESTART.20250717{13,21}_DOMAIN1` are the production land state.
- Two interactive sessions worked this project today (E54; `spinup_winter/SESSION_LOCK.txt`). Elias to decide which continues.

## What to do next, in order
0. **Lead finding 2026-09-17 evening (DECISIONS, entry 'THE THREE VALLEY-WIND DIAGNOSTICS'):** [pressure-gradient claim RETRACTED 20:50 — at fixed heights WRF holds ICON's gradient; what stands:] on 17 July a northerly floods the upper valley over the northern ranges
   from 16 UT (Innsbruck 8–9 m/s from N vs observed easterly); ICON does not do it; ICON terrain in WRF recovers the daytime
   gradient but not the break-in. Next: cross-sections of the northerly across the Karwendel/Seefeld/Achensee at 15–17 UT, WRF vs
   ICON; treat the 17th's evening as unfit for closure tests, prefer the 18th.
1. DONE 2026-09-17 (DECISIONS 15:30, 16:15): initial state clean aloft; along-valley θ flat; floor warm advection = subsidence (slope-circulation return branch); filter ~0.06 K/h at Kolsass; ICON's own floor cools −0.45 K/h vs WRF −0.28 vs obs −0.86. Hypotheses 2/3 (slope drainage + laminar core) carry the deficit. Previously: initial state checked (DECISIONS 2026-09-17 15:30): ICON at 21 UT is on HATPRO aloft (+0.2 K at 100–300 m, +0.9 K at 0–100 m) and
   on the lidar; the deficit opens 21:00–22:30 while X16s turns to a westerly down-valley flow (1.2 m/s from 272° at 50–150 m at 22 UT)
   where the lidar keeps a weak easterly (from ~95°) up to 300 m. NEXT: θ along the valley axis at 21 UT and in the first frames
   (is the western reach warmer?), the along-valley pressure force and momentum budget on X16s (`evening_momentum_budget.py X16s`,
   `valley_transect.py X16s …`), the lidar wind hour by hour (`icon_init_check.py` covers 20:30–22:30).
2. Drainage onset checked (DECISIONS 17:15): sonics show drainage established from 19–20:30 UT; the 21 UT cold start has none and X16s
   overshoots it 1.5–2× within 30 min → the floor's early warm subsidence is a cold-start transient; X17 is the test. Then item 4: slope drainage path budget and q² vs the slope sonics on X16s (crest→floor at ~9 m; where the drainage detaches, how it is diluted; the 150–300 m layer that the observations cool at −0.77 K/h under a 1.5 m/s easterly the model kills). Then item 5 (closure sensitivity twin at the drainage top). The X16w programme on X16s (`x16w_slope_diag.py X16s` section B/C is in `X16s.log`; the
   drainage detachment, dilution on the slope, along-valley import) — now without the surface confounder.
2. Land-surface parameters (first order for LW↑/T2 at the floor, second order for the pool): meadow land-use class for the
   i-Box meadows (LU 2 'dryland cropland' at Kolsass/Weerberg; forest cells at Hochhäuser/StanserJoch), ground–canopy
   insulation (top-layer swing 40 % too large, skin +2 K at night), loam hydraulics (28 vs 32 vol-%).
3. Chain start (13 UT) from `RESTART.2025071713_DOMAIN1` (winter run) via `make_twin_rundir.sh <13 UT parent> <name> …` when a
   daytime twin is wanted; grep the submit script for the E49 `mkdir` before `sbatch`.

## What the gate showed (DECISIONS 2026-09-16 11:15)
- Offline Noah-MP with the spun-up soil, ICON atmosphere, Kolsass: night skin +1 K, HFX −3 vs sonic −3, net LW 51 vs 56,
  ground flux −60 vs observed residual −48 (chain: 20–25 short); day Bowen 0.38 vs 0.43 observed (chain 0.8 with ICON's 22 %).
  → A28's soil state is the mechanism behind the chain's floor errors. Remaining: the 0–10 cm layer swings 40 % too much and
  ends the night 2–3 K colder than the 5 cm probes at all sites (ground–canopy insulation, a parameter, second order).
- Spun-up minus ICON (land mean): +2.2…+3.3 K per layer, +1.3…+3.3 vol-%. Pass 2 − pass 1: +0.1 (top) … +0.9 K (1–2 m):
  deep layers not converged — a production spin-up wants real forcing from the previous autumn (soil-moisture memory).
- Slope cells (Eggen, Weerberg, StanserJoch, Hochhäuser) keep H 1.3–4× the meadow towers: their land use is mosaic/forest — a
  land-cover question, not soil.

## What to do next, in order
1. **Judge X16s when 8637042 is done** (`exp/x16_judge/X16s.log`, or run the scripts by hand). Gates: slope HFX → −13…−15 W m⁻², GRDFLX → −41…−47, LW↑ at
   Weerberg/Hochhäuser, drainage at Eggen/StanserJoch, HATPRO 21→01 cooling (−3.4/−3.1 K), X16w θ budget terms; compare
   with X16b statistically (E14). The stations to read are in `slope_soil_check.py`, `slope_radiation_check.py`,
   `sfclay5_night_check.py`, `x16_theta_budget.py` (needs the WRFlux stream, on).
2. The chain start (13 UT) uses `spinup_winter_july/LDASOUT/RESTART.2025071713_DOMAIN1` (the winter state) the same way (`make_twin_rundir.sh <13 UT parent> <name> …`,
   its own env file `vsc5_<name>.sh`).
3. **Done 2026-09-17:** the December-start production spin-up (W_SNOW and soil ice in the setup). Still open: meadow land-use
   class, ground–canopy insulation, loam hydraulics (the parameter side of A28).

## Tools and files made this week
- `icon2wrf` main: `run_surface_series.sh START END [OUT] [JOBS] [WRFINPUT|grid.txt|none]` → one hourly NetCDF
  (HRLDAS names) on the WRF grid; `src/icon2wrf/surface_series.py`. Product: `$DATA/ICON/surface_series/
  icon_surface_2025040100_2025071800.nc` (71 GB, 2593 h, no gaps; 24 h diurnal-filled 05-11/12; ~60 h interpolated,
  17 July all real). Also on LEO4 (`ssh leo4`, key set up; `/scratch/c7071156/icon2wrf/output/`).
- `$DATA/hrldas`: HRLDAS v5.1.1 built serial + MPI (`hrldas/run/hrldas_serial.exe`, `hrldas.exe`); `noahmp/` is a
  copy of `branko/phys/noahmp` (MVT=33 fix pushed to the fork, branch `corine-33cat`, submodule bumped in branko).
  `tools/make_ldasin.py` (series → hourly LDASIN files), `tools/make_setup.py` (wrfinput + ICON land state at t0),
  `tools/hrldas_to_wrfinput.py` (restart → wrfinput land state), `tools/make_twin_rundir.sh` (X16b → X16s twin dir); all four
  mirrored in `branko/realcase/scripts/hrldas_tools/`.
- `wrf3dpbl-diag`: `hrldas_check.py` (the A28 gate: probes + EC vs HRLDAS output, daily or hourly mode; `--loop N` for a spin-up pass), `hrldas_pass_compare.py` (ICON soil / pass 1 / pass 2 / probes + ICON's own fluxes), `sfclay5_night_check.py`, `x16_theta_budget.py`, `x16w_slope_diag.py`, `slope_radiation_check.py`,
  `slope_soil_check.py`, `plot_radiation_stations.py` (figure `plot_output/diagnostics/radiation/ibox_radiation_1718.png`).
- Station data added: `data/stations/ibox_rad/` (CNR4 at Weerberg, Hochhäuser, Arbeser; raw at Eggen, StanserJoch —
  their LW columns are uncalibrated mV). Rule: never send mails / use the Gmail connector without asking.

## Standing rules that changed
- **Every future WRF run takes its land state from the full offline spin-up** (winter-start HRLDAS run; restarts in
  `hrldas_runs/spinup_winter_july/LDASOUT/RESTART.<YYYYMMDDHH>_DOMAIN1`, daily 21 UT ones in `spinup_winter/LDASOUT`) through
  `make_twin_rundir.sh` / `hrldas_to_wrfinput.py`; a raw real.exe land state is a named exception. Elias 2026-09-17; DECISIONS 14:55.
- Full output streams and `output_t_fluxes = 1` on every experiment until production.
- `submit_wrf.slurm` now creates `$WRF_OUTPUT_ROOT/temp/branko` (E49); check the initial surface state against every
  station variable before any physics hypothesis (E52, RULE-003).
- HRLDAS traps: the first LDASOUT file of a segment has −9999 fluxes (E55); station-CSV diagnostics need the conda env `proc`,
  not the WRF env's python (E56).
